# LF in the repo *and* in the working copy on every platform. Without eol=lf a Windows
# checkout gets CRLF, which fails `prettier --check` (its default endOfLine is "lf")
# even though the committed content is fine.
* text=auto eol=lf
*.sh text eol=lf
