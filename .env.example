# Copy to .env — defaults target the local Docker SUT (docker compose up -d)
# See docs/ENVIRONMENT.md. Never point automation at the public hosted demo.

SUT_BASE_URL=http://localhost:4200
SUT_API_URL=http://localhost:8091

# Demo credentials from the Toolshop seed data (not secrets, but kept in env by policy)
SUT_CUSTOMER_EMAIL=customer@practicesoftwaretesting.com
SUT_CUSTOMER_PASSWORD=welcome01
SUT_ADMIN_EMAIL=admin@practicesoftwaretesting.com
SUT_ADMIN_PASSWORD=welcome01
